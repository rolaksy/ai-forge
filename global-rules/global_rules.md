# Global Rules

This file contains global rules that apply to all projects in all workspaces.

## Rules to Follow
Always follow these rules when working on any project.

All rule files are located in the `global-rules` directory of /home/$USER/projects/KP-Xmit-WorkSpace/

### Java, Spring, Maven Projects
- Use rules from `java-spring-maven.md` rule file


## Prompts
- All prompts are located in the `prompt-library` directory of /home/$USER/projects/KP-Xmit-WorkSpace/
- Use prompts from `prompt-library` directory when working on any project.