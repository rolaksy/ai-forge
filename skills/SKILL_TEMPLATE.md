my-skill/               # directory name MUST match `name` in SKILL.md
├── SKILL.md            # required — only mandatory file
├── script.sh           # optional — scripts the skill references
├── template.md         # optional — output templates
└── examples/           # optional — example scenarios/inputs