# Assistant Instructions and Context7 Preferences

Saved on: 2026-06-17T23:25:58.772+12:00

## User preference
- Use Context7 (Context7 MCP Server) by default for all developer and documentation lookups when relevant.
- If a library or space is ambiguous, ask a brief clarifying question before proceeding.
- If the user requests exceptions (e.g., skip for trivial questions), ask which exceptions to apply.

## Context7 usage workflow
1. Resolve a library ID with `context7-resolve-library-id` when a libraryId is not already provided (required unless user gives `/org/project`).
   - Limit: call `context7-resolve-library-id` at most 3 times per question.
2. Query docs with `context7-query-docs` using the resolved `libraryId` and a natural-language question.

## Use cases for Context7 MCP Server
- Quick API lookups and usage examples
- Version-specific migration or breaking-change guidance
- Finding canonical code snippets and recipes
- Integrating internal or third-party docs for AI-assisted coding
- Reducing web searches when implementing unfamiliar libraries

## Operational notes and constraints
- Expect slightly higher latency for some queries due to Context7 lookups.
- Keep resolve-library-id calls under the documented limit (≤3 per question).
- For ambiguous or multi-library queries, prompt user for clarification with a short question.

## Where this file is stored
- File path: /home/laksyalamat/projects/git/ai-forge/copilot-cli/instructions.md

---
Generated by Copilot CLI session to persist user preferences for future lookup behavior.
