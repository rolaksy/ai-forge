# PR Code Review Response — PR #41538

**Repository:** KP-MapJava  
**Branch:** `feature/ado973273-machine-lineup-api-for-nla--laks`  
**Date:** 2026-05-27  
**Tests:** 17 / 17 passing after all changes

---

## Summary

All valid findings have been addressed. Several findings were incorrect (import already existed, annotations already present, test already implemented, patterns both required). Full details below.

---

## Implemented Fixes

| CR | File | Change |
|----|------|--------|
| CR-002 / CR-010 | `MachineLineupMapper.java` | Fixed NPE in `toDTO()` — added null check on `getMasterSpecification()` before calling `.getName()`. `toDTO()` is now consistent with `toDetailDTO()`. |
| CR-004 | `MachineLineupController.java` | Added `logger.info("Getting lineup entry: {} for machine: {}", lineupEntryId, machineNumber)` at start of `getLineupEntry()` for parity with `getLineup()`. |
| CR-006 | `MachineLineupController.java` | Added null guard on `getOrderedMaterialMaster().getId()` before passing to `List.of()`. `List.of(null)` throws NPE — this was a real risk. |
| CR-007 | `MachineLineupEntryDTO.java` | Removed redundant `@NotNull` from `orderNumber` and `status` fields. `@NotBlank` already implies not-null on `String`. |
| CR-011 | `MachineLineupMapper.java` | Same fix as CR-010 — `toDTO()` null handling is now consistent with `toDetailDTO()`. |
| CR-012 | `MachineLineupMapper.java` | Extracted hardcoded `"SCHEDULED"` string to `private static final String DEFAULT_STATUS = "SCHEDULED"` to eliminate magic string duplication across `toDTO()` and `toDetailDTO()`. |
| CR-013 | `MachineLineupMapper.java` | Fixed `getSpeedPerHour()` null risk in both `toStepDTO()` and `toDetailStepDTO()`. Added null check on `getSpeedPerHour()` in the ternary guards for `runSpeed` and `targetRunSpeed`. |
| CR-016 | `PcsAuthenticationPatternConfigurator.java` | Added missing `.mvcMatchers("/api/v1/machines/*/lineup")` pattern. `WebSecurityConfig` already had both patterns; `PcsAuthenticationPatternConfigurator` only had `/lineup/**`, leaving `GET /lineup` (the list endpoint) unauthenticated via the old security chain only. Both patterns are now consistently registered in both configs. |
| CR-018 | `TestMachineLineupController.java` | Added `testGetLineup_CapsLimitAt999()` — verifies that requesting `limit=2000` results in the service being called with `999`. |
| CR-019 | `TestMachineLineupController.java` | Added `testGetLineupEntry_ContinuesAndReturnsResult_WhenManufacturingServiceThrows()` — verifies that `ManufacturingServiceException` is swallowed and a valid response is still returned (material components are optional). |

---

## Invalid or Incorrect Findings

| CR | Reason |
|----|--------|
| **CR-001** | **Invalid.** `ManufacturingServiceException` is **already imported** at line 20: `import kiwiplan.manufacturing.service.ManufacturingServiceException;` — no compilation error exists. |
| **CR-008** | **Invalid.** `jobNumber` already has both `@NotNull` and `@Positive`. The finding says they are missing — they are not. |
| **CR-009** | **Partially invalid.** The claim that `jobNumber` lacks `@NotNull` is incorrect — it is already present. The redundant `@NotNull @NotBlank` issue on `orderNumber` and `status` is valid and was addressed under CR-007. |
| **CR-015** | **Invalid.** Both patterns are necessary. `/api/v1/machines/*/lineup` (no trailing path) and `/api/v1/machines/*/lineup/**` (entry sub-resource) serve different URL structures. Spring's `AntPathRequestMatcher` does **not** match the exact `/lineup` path when using `/**` — the trailing `**` requires at least a `/` separator. Removing either pattern would leave one endpoint uncovered. |
| **CR-017** | **Invalid.** `testGetLineup_FiltersOutEntriesWithNullStep()` **is fully implemented** with `when()`, `MockedStatic`, assertions, and a `verify()` confirming the null-step entry was never mapped. The finding is incorrect. |

---

## Acknowledged / No Change

| CR | Decision |
|----|----------|
| **CR-003** | `Integer.valueOf(machineNumber).equals(lineupEntry.getPcsMachine().getMachineNumber())` is intentionally safe — `getMachineNumber()` returns `Integer` (boxed), not `int`. Replacing with `==` would auto-unbox and risk NPE if `getMachineNumber()` returns null. The current form is correct; the boxing overhead on a per-request comparison is negligible. **No change.** |
| **CR-005** | Material components are explicitly optional — a missing manufacturing service must not fail the lineup response. This is the intended design. The catch block now uses the standard SLF4J throwable-as-last-arg pattern (`logger.warn("...", lineupEntry.getOrderNumber(), e)`) which is cleaner and avoids calling `e.getMessage()` directly. The optional-component contract is evident from the null guards and the catch boundary. |
| **CR-014** | The `*` wildcard in `/api/v1/machines/*/lineup` matches a single path segment (the machine number) — not arbitrary paths. This is correct and consistent with all other machine-scoped endpoints in the same config. **No change.** |
| **CR-020** | Step-capacity and machine-number validation paths are each covered by dedicated negative tests (`testGetLineupEntry_ThrowsInvalidLineupMachineException_WhenMatchingEntryIsCapacityReserved`, `testGetLineupEntry_ThrowsInvalidLineupMachineException_WhenEntryBelongsToDifferentMachine`). Coverage is adequate. **No change.** |
